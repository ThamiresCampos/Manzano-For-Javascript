function numero() {
let numero = parseInt(promp("Insira um número: "));

for (var contadora = 1; contadora <= 10; contadora++) {
    
}
}
document.write(numero + " x " + contadora + " = " + contadora * numero + "<br>");