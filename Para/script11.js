var fatorial = 1;

for(contadora = 0; contadora <= 10; contadora++) {
    if (contadora % 2 == 1)
    var fatorial = contadora * fatorial (contadora - 1);
    document.write("o fatorial dos números ímpares na faixa 1 a 10 é: "+fatorial);
}